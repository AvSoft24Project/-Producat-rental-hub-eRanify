CREATE TABLE USERS(
	ID BIGINT NOT NULL,
	NAME VARCHAR(255),
	PHONE_NUMBER VARCHAR(20),
	PASSWORD VARCHAR(255),
	ADDRESS VARCHAR(255),
	EMAIL_ID VARCHAR (255),
	PRIMARY KEY(ID)
)
select * from USERS

CREATE TABLE PRODUCT(
	ID INT PRIMARY KEY,
	NAME VARCHAR(255),
	DISCRIPTION TEXT,
	PRICE DECIMAL(10,2),
	DISCOUNT DECIMAL(5,2),
	IMAGE VARCHAR(255),
	USER_ID INT,
	CATEGORY_ID INT,
	FOREIGN KEY(user_id)REFERENCES USERS(id),
	Foreign KEY(category_id)REFERENCES CATEGORIES(ID)
)
select * from product
	
CREATE TABLE BOOKING(
	ID INT PRIMARY KEY,
	TOTAL_PRICE DECIMAL(10,2),
	START_TIME VARCHAR(255),
	END_TIME VARCHAR(255),
	BOOKING_STATUS VARCHAR(50),
	USER_ID INT,
	FOREIGN KEY(user_id)REFERENCES USERS(id)
)
select * from booking
 
CREATE TABLE CATEGORYS(
	ID INT PRIMARY KEY,
	TITLE VARCHAR(255),
	DESCRIPTION TEXT
)
select * from categorys

CREATE TABLE REVIEW(
	ID INT PRIMARY KEY,
	COMMENT TEXT,
	RATING INT,
	USER_ID INT,
	PRODUCT_ID INT,
	FOREIGN KEY(user_id)REFERANCES USERS(ID),
	FOREIGN KEY(product_id)REFERANCES PRODUCTS(ID)
)
select * from review